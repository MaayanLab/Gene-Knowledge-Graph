{
	"nodes": [
		{
			"node": "Gene",
			"route": "/genes",
			"example": {
				"href": {
					"pathname": "/",
					"query": {
						"start": "Gene",
						"start_term": "GCC2"
					}
				}, 
				"palette": "greens"
			},
			"node_color": "#aed581"
		}
	],
	"order": {
		"Confidence_Values": {
			"on": "edge",
			"field": ["Confidence_Values", "DESC"]
		}
	},
	"tooltip": {
		"Gene": [
			{
				"label": "Gene ID",
				"text": "${id}"
			},
			{
				"label": "Aliases",
				"text": "${Aliases.split('|').join(', ')}"
			},
			{
				"label": "Alternative IDs",
				"text": "${Alt_IDs.split('|').join(', ')}"
			}
		],
		"Relation": [
			{
				"label": "Gene ID",
				"text": "${id}"
			},
			{
				"label": "Source Label",
				"text": "${source_label}"
			},
			{
				"label": "Target Label",
				"text": "${target_label}"
			},
			{
				"label": "Source Database",
				"text": "${Source_Database}"
			},
			{
				"label": "Interaction Detection Method",
				"text": "${Interaction_Detection_Method}"
			},
			{
				"label": "Publication First Author",
				"text": "${Publication_1st_Author}"
			},
			{
				"label": "Pubmed",
				"text": "${Publication_Identifiers}"
			},
			{
				"label": "Alternative IDs",
				"text": "${Alt_IDs.split('|').join(',')}"
			},
			{
				"label": "Confidence Value",
				"text": "${Confidence_Values}"
			}
		]
	},
	"header": {
		"title": "Gene-centric Knowledge Graph",
		"subheader": [
			{
				"label": "BioGRID",
				"icon": "${PREFIX}/static/resources/biogrid.jpg"
			},
			{
				"label": "ARCHS4",
				"icon": "${PREFIX}/static/resources/archs.png"
			},
			{
				"label": "LINCS",
				"icon": "${PREFIX}/static/resources/lincslogo.svg"
			}
		]
	}
}